============
Installation
============

At the command line::

    $ easy_install rocketdiagram

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv rocketdiagram
    $ pip install rocketdiagram
